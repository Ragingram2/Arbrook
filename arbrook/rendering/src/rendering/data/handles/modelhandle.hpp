#pragma once
#include "rendering/data/model.hpp"

namespace rythe::rendering
{
	//struct model_handle
	//{
	//	model* m_model = nullptr;
	//	model_handle() = default;
	//	model_handle(std::nullptr_t null_ptr) : m_model(null_ptr) {}
	//	model_handle(model* _model) : m_model(_model) {}
	//	model_handle(model_handle& handle) : m_model(handle.m_model){}

	//	struct model* operator->() { return m_model; }
	//	operator struct model& () const { return *m_model; }
	//};
}